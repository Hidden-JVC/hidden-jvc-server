const isModerator = require('./isModerator.js');
const authenticate = require('./authenticate.js');
const authRequired = require('./authRequired.js');

module.exports = {
    isModerator,
    authenticate,
    authRequired
};
